# Sheldrake Field Project — TAE v45 / Results Console v22 handoff

Updated: 2026-09-16T20:48Z. Purpose: let a fresh Claude, Cursor, or Codex session continue safely after the v45/v22 release.

## Start here
- Repository: `C:\Users\atsak\Downloads\ad-worktree\foi-projects-roots\SheldrakeFieldProjectRoot`; branch `cursor/tae-v9`.
- First run `git pull`, then read root `AGENTS.md` and `magno-sensor/MAGLAB.md`; they are authoritative.
- Working tree was clean and matched `origin/cursor/tae-v9` at `23929302dcb3819d43cef820020f606ba139ca44`.
- Current release: MagLab app 59, TAE 45, analyzer 55, results/pair browser 22, command console 22, protocol 3, Room schema 10.
- Tags: `analyzer-v54` (refresh queue fix), `analyzer-v55` (canonical scoring/console), `quietcheck-v45` (app).

## What shipped
- Results table body scrolls beneath a sticky header; main table has 12 columns.
- Sat higher, Ended up, Weave, Readable, and Combined moved into expanded run details; Move ÷ wobble remains the compact compass-path diagnostic.
- Main score columns are Compass and Compass tail. Unreadable Compass displays gray `0.0σ`, is omitted from Fisher, and retains the existing run-level Unclear safety rule.
- Compass tail now turns purple and enters Fisher at **1.5σ**; Compass is 1.5σ; Accelerometer/Gyroscope are 2.0σ.
- Info buttons on score/result headers show meaning, formula, time windows, gate, units, recipe id, formula version, and heuristic SHA-256.
- TAE v45 records `published_recipe_inputs`; the console recomputes all four public scores rather than copying phone `z_*` values.
- Pre-v45 immutable runs use explicitly labeled compatibility fallbacks in details; raw phone values remain diagnostics only.
- Python and Kotlin now use the same normal-CDF approximation and shared frozen goldens, preventing numerically different “same” recipes.
- “Check for new runs” no longer returns a misleading “job already running”: analyzer v54 queues one refresh behind an active job; duplicate queued clicks coalesce.

## Canonical scoring contract
- Source of truth: `magno-sensor/heuristics/v10.json` (v29 / `tae-sigma-v29`); APK asset copy must be byte-identical.
- Implementations: `quietcheck/.../TaeMeter.kt` dispatchers and `analyzer/db/tae_recipes.py`; console path is `tae_rescore.py` → named recipes.
- Frozen parity fixture: `magno-sensor/heuristics/recipe_goldens.json`; tests: `TaeRecipeLockTest.kt` and `tests/test_tae_recipes.py`.
- Recipe ids: field `weave-gated-field-v29`; ease `late-minus-tail-v29`; jump `weighted-excursion-v28`; twist `gyro-quiet-minute-v28`.
- Never modify recorded run data. Recipe changes must update JSON id/parameters, both runtimes, phone input capture, explanations, and parity tests together.
- Detailed policy: `magno-sensor/docs/maglab/scoring.md`; app state: `magno-sensor/docs/maglab/app.md`.

## Verification and live state
- Full analyzer suite: 277 passed. Full TAE suite: 172 passed. Targeted Python parity/export tests: 44 passed. Generated browser JavaScript parsed successfully.
- Live refresh completed return code 0 at 2026-09-16 13:41 PDT; live v22 markers, sticky header, explanations, 1.5σ gate, and gray-zero copy were verified.
- Results: https://futureofinquiry.org/maglab/
- TAE install: https://futureofinquiry.org/field-meter/maglab/
- APK: `TAE-v45.apk`, 1,472,585 bytes, SHA-256 `3940cae68b0aa6ea59f4201b39d0e8b9ca9daffab86f41b8c9e7ba1222fe5a9c`.
- Analyzer deploy reported Signal Beyond active and untouched.

## Continue safely
- No unresolved issue remains from this release. Project backlog is in `magno-sensor/docs/TAE_WORK_ORDER_V41.md`, `TAE_CODE_ISSUES.md`, `TAE_NEXT_CUT.md`, `TAE_FALLOFF.md`, `TAE_GYRO.md`, and `TAE_ACCEL.md`; do not assume all backlog items are approved.
- Use `magno-sensor/tools/release.ps1`, never hand-release. For a constrained release, do **not** combine an explicit version with `-Auto`; that auto-includes every changed component.
- Analyzer and TAE were intentionally released sequentially so both have reliable component tags. A future recipe change normally requires both releases again.
- Shared hosting is the instrument/system of record; FTP only. Never touch Signal Beyond, its nginx block, or port 3000. Analyzer run fetch is FTPS-first and its credential file must never be printed or changed.
- The public results page is regenerated by POSTing `/maglab/api/refresh`; inspect `/maglab/api/job` until return code 0. Do not edit generated HTML.
